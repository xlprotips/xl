
#[derive(Debug)]
pub struct Worksheet {
    pub name: String,
    pub position: u8,
    pub id: String,
    // _used_area: 
    // pub row_length: u16,
    // pub num_rows: u32,
    // pub workbook: Workbook,
    // pub name: String,
    // pub position: u8,
    // pub location_in_zip_file: String,
}
